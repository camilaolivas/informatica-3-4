#include <stdio.h>

int main (void)
{
    int number;
    //We delteted the exclamation sign
    int test_value;
    //We used and underscore
    int example1;
    //We changed the number so the sentence start with a letter
    float maria;
    //We deleted int and add the name of the variable
    printf("Variables created successfully.\n");./.
}
